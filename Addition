#include<stdio.h>
  int main()
{
  int num1,num2, add;
printf("enter the number:");
scanf("%d",& num1);
printf("enter the number :");
scanf("%d",&num2);
  add = num1+num2;
  printf("%d+%d=%d",num1,num2,add);
  return 0;
}
